<UserControl
	x:Class="FantaAsta.Views.PrezzoView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:conv="clr-namespace:FantaAsta.Resources.Converters"
	xmlns:prism="http://prismlibrary.com/"
	prism:ViewModelLocator.AutoWireViewModel="True"
	DataContextChanged="OnDataContextChanged">

	<UserControl.Resources>
		<conv:StringDoubleConverter x:Key="StringDoubleConv"/>
	</UserControl.Resources>

	<UserControl.InputBindings>
		<KeyBinding Key="Return" Command="{Binding ConfermaCommand}"/>
		<KeyBinding Key="Esc" Command="{Binding AnnullaCommand}"/>
	</UserControl.InputBindings>

	<Grid FocusManager.FocusedElement="{Binding ElementName=prezzoTxtBox}">
		<TextBox Grid.Row="1" Name="prezzoTxtBox" Text="{Binding Prezzo, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource StringDoubleConv}}" FontSize="24" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Width="100" Height="65"/>
	</Grid>
	
</UserControl>
