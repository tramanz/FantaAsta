<UserControl
	x:Class="FantaAsta.Views.OpzioniView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:prism="http://prismlibrary.com/"
	prism:ViewModelLocator.AutoWireViewModel="True">

	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition/>
			<ColumnDefinition/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition/>
		</Grid.RowDefinitions>

		<Label Grid.Row="0" Grid.Column="0" Content="Budget iniziale:" FontSize="24" VerticalAlignment="Center"/>
		<Grid Grid.Row="0" Grid.Column="1" Height="45" Margin="40,0,0,0">
			<Grid.ColumnDefinitions>
				<ColumnDefinition/>
				<ColumnDefinition/>
				<ColumnDefinition/>
			</Grid.ColumnDefinitions>

			<RepeatButton Grid.Column="0" Style="{StaticResource IconRepeatButton}" Content="{StaticResource MinusIcon}" Command="{Binding DiminuisciBudgetCommand}" Margin="0,0,5,0"/>
			<TextBox Grid.Column="1" Name="budgetTxtBox" Text="{Binding Budget, UpdateSourceTrigger=PropertyChanged}" FontSize="24" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Width="100"/>
			<RepeatButton Grid.Column="2" Style="{StaticResource IconRepeatButton}" Content="{StaticResource PlusIcon}" Command="{Binding AumentaBudgetCommand}" Margin="5,0,0,0"/>
		</Grid>

	</Grid>

</UserControl>