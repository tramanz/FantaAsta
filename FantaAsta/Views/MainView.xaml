<UserControl
	x:Class="FantaAsta.Views.MainView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:prism="http://prismlibrary.com/"
	prism:ViewModelLocator.AutoWireViewModel="True">

    <Grid>
        <TabControl prism:RegionManager.RegionName="ContentRegion" Background="LightGray" Margin="0,4,0,0"/>
        <Button Content="Indietro" Command="{Binding IndietroCommand}" FontSize="18" HorizontalAlignment="Right" VerticalAlignment="Top" Height="35" Width="100" Margin="0,4,8,0"/>
    </Grid>

</UserControl>
