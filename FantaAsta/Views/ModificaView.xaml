<UserControl
	x:Class="FantaAsta.Views.ModificaView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:prism="http://prismlibrary.com/"
    prism:ViewModelLocator.AutoWireViewModel="True">

	<Grid Margin="10" HorizontalAlignment="Stretch">
		<Grid.RowDefinitions>
			<RowDefinition/>
			<RowDefinition/>
			<RowDefinition/>
			<RowDefinition/>
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition/>
			<ColumnDefinition/>
		</Grid.ColumnDefinitions>

		<ComboBox Grid.Row="0" Grid.Column="0" ItemsSource="{Binding Svincolati}" SelectedItem="{Binding SvincolatoSelezionato}" Width="250" Height="25" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,5,5"/>
		<TextBox Grid.Row="1" Grid.Column="0" Text="{Binding Prezzo, UpdateSourceTrigger=PropertyChanged}" Width="100" Height="50" FontSize="24" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,5,5,5"/>
		<Button Grid.Row="2" Grid.Column="0" Content="Aggiungi" Command="{Binding AggiungiCommand}" Width="100" Height="25" Margin="0,5,5,5"/>
		<ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding Rosa}" SelectedItem="{Binding GiocatoreSelezionato}" Width="250" Height="25" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5,0,0,5"/>
		<Button Grid.Row="2" Grid.Column="1" Content="Rimuovi" Command="{Binding RimuoviCommand}" Width="100" Height="25" Margin="5,5,0,5"/>

		<Button Grid.Row="3" Grid.ColumnSpan="2" Content="Chiudi" Command="{Binding ChiudiCommand}" Width="100" Height="25" Margin="0,5,0,0"/>
	</Grid>

</UserControl>
