<UserControl
	x:Class="FantaAsta.Views.ModificaView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:conv="clr-namespace:FantaAsta.Converters"
    xmlns:prism="http://prismlibrary.com/"
    prism:ViewModelLocator.AutoWireViewModel="True">

    <UserControl.Resources>
        <conv:StringDoubleConverter x:Key="StringDouble"/>
    </UserControl.Resources>
    
    <Grid Margin="10" HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
			<ColumnDefinition Width="Auto"/>
		</Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Svincolati" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,5"/>
        <ComboBox Grid.Row="1" Grid.Column="0" ItemsSource="{Binding Svincolati}" SelectedItem="{Binding SvincolatoSelezionato}" DisplayMemberPath="Nome" Width="250" Height="25" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,5,5,5"/>
        <TextBox Grid.Row="2" Grid.Column="0" Text="{Binding PrezzoAcquisto, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource StringDouble}}" Width="100" Height="50" FontSize="24" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,5"/>
        <Button Grid.Row="3" Grid.Column="0" Content="Aggiungi" Command="{Binding AggiungiCommand}" Width="100" Height="25" FontSize="12" Margin="0,5"/>

        <Label Grid.Row="0" Grid.Column="1" Content="Rosa" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,5"/>
        <ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding Rosa}" SelectedItem="{Binding GiocatoreSelezionato}" DisplayMemberPath="Nome" Width="250" Height="25" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5,5,0,5"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding PrezzoVendita, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource StringDouble}}" Width="100" Height="50" FontSize="24" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,5"/>
        <Button Grid.Row="3" Grid.Column="1" Content="Rimuovi" Command="{Binding RimuoviCommand}" Width="100" Height="25" FontSize="12" Margin="0,5"/>
        
        <Button Grid.Row="4" Grid.ColumnSpan="2" Content="Chiudi" Command="{Binding ChiudiCommand}" Width="100" Height="25" FontSize="12" Margin="0,5,0,0"/>
    </Grid>

</UserControl>
